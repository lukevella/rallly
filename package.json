{
  "name": "rallly-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "db:deploy": "turbo db:deploy",
    "test": "turbo test",
    "lint": "turbo lint",
    "lint:tsc": "turbo lint:tsc"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.8.3"
  }
}
