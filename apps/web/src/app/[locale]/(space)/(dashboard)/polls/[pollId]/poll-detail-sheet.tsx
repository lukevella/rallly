"use client";

// Shell-scoped mount point for the detail sheet driven by ?responseId= /
// ?optionId=. It mounts once from the poll layout so every poll page shares
// one sheet; content and history behavior land with the detail sheets issue.
export function PollDetailSheet() {
  return null;
}
